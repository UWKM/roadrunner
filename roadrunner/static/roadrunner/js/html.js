var HtmlCache = {
'StreamBlock.html': '<div class="rr_streamblock" data-treepath="{{tree_path}}">    <input type="hidden" name="{{path}}-count"   value="{{count}}" />    <input type="hidden" name="{{path}}-order"   value="{{order}}" />    <input type="hidden" name="{{path}}-deleted" value="{{deleted}}" />    <div class="children"></div>    <button data-action="addChild"/>    <div class="addChild"></div></div>',
};
